#include <stdio.h>

/**
 * main - print alphabet on single line
 * Return: 0 to exit
 */

int main(void)
{
	char l;

	while (l = 'a'; l <= 'z')
	{
		putchar(x);
		l++;
	}
	putchar('\n');
	return (0);
}
