#include <stdio.h>
#include "holberton.h"

/**
 * _abs - compute the absolute value
 * @int: integer in question
 *
 * Return: 0
 */

in _abs(int)
{
	int n;
	int p;

	if (int >= 0)
	{
		_putchar(int);
	}
	else if (int < 0)
	{
		n = int;
		p = -n;
		_putchar(p);
	}
	return(0);
}
